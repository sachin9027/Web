@model List<Web.Connectiondata.radika>


<style>
    .error {
        color: #ff6a00
    }
</style>
<h1>************Employee Details**********</h1>
<a href="@Url.Action("form","Home")">Create</a>
<table class="table table-hover table-bordered form-color table-danger">


    <tr>

        <th>id</th>
        <th>name</th>
        <th>age </th>
        <th>city </th>
        <th>salary</th>
        <th>Action</th>
        <th>Action</th>



    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>@item.id</td>
            <td>@item.name</td>
            <td>@item.age</td>
            <td>@item.city</td>
            <td>@item.salary</td>





            <td><a href="@Url.Action("Delete","Home",new { item.id })" class="btn btn-danger">delete</a></td>
                <td><a href="@Url.Action("edit","Home",new { item.id })" class="btn btn-danger">edit</a></td>
        </tr>
    }


</table>